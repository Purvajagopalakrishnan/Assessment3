@model IEnumerable<BooksLibrary.Models.Books>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                Name
            </th>
            <th>
               Quantity
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Book_Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Quantity)
            </td>
            
        </tr>
}
    </tbody>
</table>
<html>
<body>
    @Html.ActionLink("Borrow", "Edit", new { /* id=item.PrimaryKey */ }) |
    @Html.ActionLink("Addbooks", "Addbooks", new { /* id=item.PrimaryKey */ })

</body>
</html>
